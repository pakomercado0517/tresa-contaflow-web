/**
 * Funcionalidades que siguen en código pero no se muestran en la UI
 * hasta que estén listas. Cambiar a `true` para volver a habilitarlas.
 */
export const PRODUCT_FEATURES = {
  taxEstimate: false,
  satDownload: false,
} as const;
